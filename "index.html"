<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VECTOR K9 | Behavioral Logistics Jacksonville</title>
    <meta name="description" content="Precision dog training for Nocatee, Ponte Vedra, and Jacksonville. Specializing in aggression rehabilitation and off-leash reliability.">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&family=Oswald:wght@400;700&display=swap" rel="stylesheet">

    <style>
        /* --- THE DESIGN SYSTEM --- */
        :root {
            --void: #050505;       /* Deep Black */
            --concrete: #1a1a1a;   /* Dark Grey Surface */
            --steel: #888888;      /* Text Muted */
            --flash: #F0F0F0;      /* Text Bright */
            --hazard: #FF3300;     /* International Orange */

            --font-head: 'Oswald', sans-serif;
            --font-body: 'Inter', sans-serif;
        }

        /* --- RESET & BASE --- */
        * { box-sizing: border-box; margin: 0; padding: 0; }

        body {
            background-color: var(--void);
            color: var(--flash);
            font-family: var(--font-body);
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
        }

        a { text-decoration: none; color: inherit; transition: 0.3s; }
        ul { list-style: none; }

        /* --- TYPOGRAPHY --- */
        h1, h2, h3 {
            font-family: var(--font-head);
            text-transform: uppercase;
            letter-spacing: 2px;
            font-weight: 700;
        }

        h1 { font-size: 3.5rem; line-height: 1.1; margin-bottom: 1rem; }
        h2 { font-size: 2.5rem; color: var(--flash); margin-bottom: 2rem; }
        p { color: var(--steel); max-width: 60ch; margin-bottom: 1.5rem; }

        .highlight { color: var(--hazard); }

        /* --- LAYOUT UTILITIES --- */
        .container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 0 20px;
        }

        section { padding: 80px 0; border-bottom: 1px solid #222; }

        /* --- COMPONENTS --- */
        .btn {
            display: inline-block;
            background-color: var(--hazard);
            color: var(--void);
            font-family: var(--font-head);
            font-weight: 700;
            padding: 15px 35px;
            text-transform: uppercase;
            letter-spacing: 1px;
            border: 1px solid var(--hazard);
            cursor: pointer;
            transition: 0.3s;
        }

        .btn:hover {
            background-color: transparent;
            color: var(--hazard);
        }

        /* --- HERO SECTION --- */
        .hero {
            height: 90vh;
            display: flex;
            align-items: center;
            background: linear-gradient(rgba(5,5,5,0.7), rgba(5,5,5,0.9)),
                        url('https://images.unsplash.com/photo-1633722715463-d30628519d51?w=1200&h=800&fit=crop');
            background-size: cover;
            background-position: center;
        }

        /* --- PHILOSOPHY GRID --- */
        .grid-3 {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 40px;
            margin-top: 40px;
        }

        .card {
            background: var(--concrete);
            padding: 30px;
            border-left: 2px solid var(--hazard);
        }

        .card h3 { margin-bottom: 10px; color: var(--flash); }

        /* --- CONTACT FORM --- */
        .form-box {
            background: var(--concrete);
            padding: 40px;
            max-width: 600px;
        }

        label {
            display: block;
            font-family: var(--font-head);
            color: var(--steel);
            font-size: 0.9rem;
            margin-bottom: 5px;
            letter-spacing: 1px;
        }

        input, select {
            width: 100%;
            background: var(--void);
            border: 1px solid #333;
            color: var(--flash);
            padding: 15px;
            margin-bottom: 20px;
            font-family: var(--font-body);
        }

        input:focus, select:focus {
            outline: none;
            border-color: var(--hazard);
        }

        /* --- MOBILE RESPONSIVENESS --- */
        @media (max-width: 768px) {
            h1 { font-size: 2.5rem; }
            h2 { font-size: 1.8rem; }
            section { padding: 50px 0; }
            .hero { height: 70vh; }
        }
    </style>
</head>
<body>

<!-- NAVIGATION -->
<nav style="padding: 20px 0; border-bottom: 1px solid #222;">
    <div class="container" style="display: flex; justify-content: space-between; align-items: center;">
        <div style="font-family: var(--font-head); font-weight: 700; font-size: 1.5rem; letter-spacing: 2px;">
            VECTOR <span class="highlight">K9</span>
        </div>
        <a href="#contact" style="font-family: var(--font-head); font-size: 0.9rem; color: var(--steel);">INTAKE STATUS: <span class="highlight">OPEN</span></a>
    </div>
</nav>

<!-- HERO SECTION -->
<header class="hero">
    <div class="container">
        <h1>RELIABILITY IS NOT<br>AN ACCIDENT.</h1>
        <p>We build systems for high-drive dogs and complex behaviors. Serving Nocatee, Ponte Vedra, and the Beaches.</p>
        <a href="#contact" class="btn">Start The Protocol</a>
    </div>
</header>

<!-- PHILOSOPHY SECTION -->
<section>
    <div class="container">
        <h2>STOP MANAGING CHAOS.</h2>
        <p>Most training fails because it relies on bribery. We rely on clarity. We install a communication system that works in the real world—past the squirrel, through the crowd, and off the leash.</p>

        <div class="grid-3">
            <div class="card">
                <h3>01 // NEUTRALITY</h3>
                <p>Exposure implies safety. We teach your dog to exist calmly in high-stress urban environments.</p>
            </div>
            <div class="card">
                <h3>02 // CLARITY</h3>
                <p>Binary feedback. Yes means yes. No means no. Anxiety vanishes when the dog understands the rules.</p>
            </div>
            <div class="card">
                <h3>03 // FREEDOM</h3>
                <p>A reliable dog is a free dog. Our goal is 100% off-leash control, anywhere in Florida.</p>
            </div>
        </div>
    </div>
</section>

<!-- CONTACT SECTION -->
<section id="contact">
    <div class="container">
        <div style="display: flex; flex-wrap: wrap; gap: 50px;">
            <div style="flex: 1; min-width: 300px;">
                <h2>INITIATE INTAKE</h2>
                <p>We do not accept every dog. We prioritize owners who are committed to the process.</p>
                <p style="border-left: 2px solid var(--steel); padding-left: 15px;">
                    <strong>Service Area:</strong><br>
                    32081 (Nocatee)<br>
                    32082 (Ponte Vedra)<br>
                    32250 (Jax Beach)
                </p>
            </div>

            <div class="form-box" style="flex: 1; min-width: 300px;">
                <form name="k9-intake" method="POST" netlify>
                    <label for="name">HANDLER NAME</label>
                    <input type="text" id="name" name="name" required placeholder="Full Name">

                    <label for="email">CONTACT EMAIL</label>
                    <input type="email" id="email" name="email" required placeholder="email@address.com">

                    <label for="dog-info">DOG BREED & AGE</label>
                    <input type="text" id="dog-info" name="dog-info" required placeholder="e.g. 2yo German Shepherd">

                    <label for="goal">PRIMARY OBJECTIVE</label>
                    <select id="goal" name="goal" required>
                        <option value="">-- Select One --</option>
                        <option value="puppy">Puppy Foundation (Future Proofing)</option>
                        <option value="behavior">Behavior Modification (Aggression/Anxiety)</option>
                        <option value="offleash">Off-Leash Reliability</option>
                    </select>

                    <button type="submit" class="btn" style="width: 100%;">Submit Application</button>
                </form>
            </div>
        </div>
    </div>
</section>

<!-- FOOTER -->
<footer style="padding: 40px 0; background: #000; color: #444; font-size: 0.8rem; text-align: center;">
    <div class="container">
        <p>&copy; 2026 VECTOR K9 SYSTEMS. JACKSONVILLE, FL.</p>
    </div>
</footer>

</body>
</html>